export const publicRoutes = ["/events"];

export const authRoutes = ["/login"];

export const apiAuthPrefix = "/api/authentication";
export const apiPrefix = "/api";

export const DEFAULT_LOGIN_REDIRECT = "/login";
export const DEFAULT_ADMIN_REDIRECT = "/admin";
export const DEFAULT_EMPLOYEE_REDIRECT = "/employee";
